/**
 * Context Index
 * Export all contexts and custom hooks for easy imports
 */

export { AuthContext, AuthProvider, useAuth } from './AuthContext';
export { CartContext, CartProvider, useCart } from './CartContext';
